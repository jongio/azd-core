# azdext Alignment: Tasks

Spec: [spec.md](./spec.md)

Legend: `[ ]` pending, `[~]` in progress, `[x]` done, `[-]` deliberately not done with a documented reason. Rows with no marker are pending. Repos: **C** azd-core, **A** azd-app, **P** azd-copilot, **R** azd-rest.

## Phase 0: Baseline

| # | Repo | Task | Depends on |
|---|---|---|---|
| [x] 0.1 | A P R | Bump `github.com/azure/azure-dev/cli/azd` to `v1.29.0`, `go mod tidy`, verify build and tests green with no code changes | |
| [x] 0.2 | R | Align `go` directive to `1.26.5` to match the other repos | 0.1 |
| [x] 0.3 | C | Move `EXTENSION_FRAMEWORK_GAP_ANALYSIS.md` to `docs/archive/` with a superseded-by note referencing azure-dev PR #6856 | |
| [x] 0.4 | C | Add `docs/sdk-upgrade-policy.md`: track latest stable `cli/azd/vX.Y.Z` tag, never pseudo-versions, never `main` | |

Phase 0 notes:

- 0.1: azd-copilot and azd-rest were already on `v1.29.0`. azd-app moved `v1.28.1` to `v1.29.0` with no source changes; build, tests, and the coverage gate stayed green at 65.0%.
- 0.2: already satisfied. All four repos declare `go 1.26.5`.
- 0.3: archived to `docs/archive/extension-framework-gap-analysis.md` with a superseded-by banner. `spec.md` now points at the new path.
- 0.4: `docs/sdk-upgrade-policy.md`. All four repos move the SDK pin together; `azd-core` does not depend on the SDK directly yet.

## Phase 1: Entry points and structured errors

Order: R, then P, then A.

| # | Repo | Task | Depends on |
|---|---|---|---|
| [x] 1.1 | R | Replace `main()` with `azdext.Run(cmd.NewRootCmd())`. Delete `ExitCoder` and manual error printing in `src/cmd/rest/main.go` and `src/internal/cmd/env.go` | 0.1 |
| [x] 1.2 | R | Add `src/internal/cmd/errors.go` with `const` snake_case error codes. Convert command-handler failures to `azdext.LocalError` / `azdext.ServiceError` with categories and suggestions | 1.1 |
| [x] 1.3 | R | Add a startup test asserting `azdext.ValidateNoReservedFlagConflicts(NewRootCmd())` returns nil | 1.1 |
| [x] 1.4 | P | Replace `main()` with `azdext.Run(newRootCmd())`. Delete manual `Execute()` error handling in `src/cmd/copilot/main.go` | 0.1 |
| [x] 1.5 | P | Add error codes and convert handler failures to structured errors | 1.4 |
| [x] 1.6 | P | Add reserved-flag conflict test | 1.4 |
| [-] 1.7 | P | Add `azdext.NewVersionCommand("jongio.azd.copilot", ...)` | 1.4 |
| [x] 1.8 | A | Replace `main()` with `azdext.Run(rootCmd)`. Preserve the chained `PersistentPreRunE` behavior | 0.1 |
| [x] 1.9 | A | Add error codes and convert handler failures to structured errors | 1.8 |
| [x] 1.10 | A | Add reserved-flag conflict test | 1.8 |
| [-] 1.11 | A | Add `azdext.NewVersionCommand("jongio.azd.app", ...)` | 1.8 |

Phase 1 notes:

- 1.1-1.3: azd-rest, commit `5499505`. Six snake_case codes in `src/internal/service/errors.go`, `fail.go` deleted, five test files moved off `ExitCode()` assertions. `RequestOptions.URL` gave a real host for the `serviceName` telemetry argument via a new `hostFromURL` helper, so nothing passes an empty string. Coverage 86.3% -> 87.0%.
- 1.4-1.6: azd-copilot, commit `9a96db4`. Five codes in `commands/errors.go`. Coverage 30.0% -> 31.4%.
- 1.8-1.10: azd-app, commit `9af66050`. Five codes in `commands/errors.go`. Coverage 65.0% -> 65.1%, and `cmd/app` went 0.0% -> 24.0% because extracting `newRootCmd` made the command tree testable.
- 1.7 and 1.11 rejected. `azdext.NewVersionCommand` is a regression against azd-core's `version.NewCommand`, not an upgrade. The SDK command emits only `{name, version}` in JSON, prints `id version` as text, and has no `--quiet`. azd-core's emits the full `Info` (version, buildDate, gitCommit, extensionId, name), prints four labeled fields, and has `--quiet`/`-q`. The SDK command's one real advantage is that it calls `RegisterFlagOptions` so azd can discover `--output json`. Task 2.5 folds that advantage into azd-core's command instead, and moves azd-rest back onto it. `spec.md` line 58 previously claimed the two were equivalent; that claim is corrected.
- Adopting `azdext.Run` was not cosmetic. azd-copilot and azd-app both called `rootCmd.Execute()`, which runs the command tree on `context.Background()` with no azd trace propagation and no gRPC access token. `azdext.Run` uses `ExecuteContext(NewContext())` with `WithAccessToken`.
- 1.10 found a shipping bug rather than passing. Five azd-app subcommands defined local flags that shadowed azd reserved globals, so `azdext.Run` would have refused to start. The collisions were already breaking `--output` and `-e` silently, because cobra resolves a local flag ahead of an inherited one. Fixed as a breaking flag rename; see the azd-app CHANGELOG.
## Phase 2: azd-core rebase

Each task must land with all three extensions still building against the prior `azd-core` tag.

| # | Repo | Task | Depends on |
|---|---|---|---|
| [x] 2.0 | C | Add `github.com/azure/azure-dev/cli/azd v1.29.0` to `go.mod` | 0.1 |
| [x] 2.1 | C | Delete `procutil`. Callers move to `azdext.IsProcessRunning` / `GetProcessInfo` | 2.0 |
| [x] 2.2 | C | Delete `shellutil`, but **not** for the reason originally stated. `azdext.DetectShell()` answers a different question (which shell the user is in) than `shellutil.DetectShell(scriptPath)` (which interpreter runs this script file), so it is not a replacement. `shellutil` is deleted because it has zero callers in azd-core and all three extensions. `cmdutil.GetDefaultShell` / `prepareHookCommand` are **kept** for the same non-equivalence reason. See `migration.md` | 2.0 |
| [x] 2.3 | C | **Trim** `pathutil` rather than delete it. Only `FindToolInPath` has an SDK equivalent, and moving to `azdext.LookupTool` fixes a real Windows bug (the old helper appended `.exe`, so `npm`, `pnpm`, `az`, and `func` were reported missing). `SearchToolInSystemPath`, `GetInstallSuggestion`, and `RefreshPATH` have no SDK equivalent and are kept. See `migration.md` | 2.0 |
| [x] 2.4 | C | Delete `azdextutil`. **Verified: zero callers** in azd-core and all three extensions, and the package was already deprecated in v0.5.3, so nothing migrates. The plan's stated replacements do not hold on inspection: `azdext.ValidateScriptName` is a shell-metacharacter denylist for script filenames, not a shell-name allowlist; `MCPServerBuilder.WithRateLimit` is bound to a builder and is not a standalone limiter; `azdext.GetProjectDir` fails with `ErrProjectNotFound` where the azd-core version fell back to the working directory. Recorded in `migration.md`. | 2.0 |
| [x] 2.5 | C | Keep `version`. `NewCommand` now calls `azdext.RegisterFlagOptions` so azd gets completion, help text, metadata, and parse-time validation for the output flag, which is the one thing `azdext.NewVersionCommand` did better. Signature note: `RegisterFlagOptions` takes two arguments, not three. azd-rest moved back onto `version.NewCommand`, recovering `--quiet`, `buildDate`, and `gitCommit`. Two latent defects surfaced doing it, both fixed: `NewCommand` hardcoded the `-q` shorthand, which panics in any extension that already binds `-q` (azd-rest uses it for `--query`), and it hardcoded the flag name `output`, which is wrong for an extension whose output is driven by its own flag. Both are now `Option` values. | 2.0 |
| [x] 2.6 | C | **Keep `urlutil`.** Verified: it is not an SSRF control and `azdext.SSRFGuard` is not a URL validator, so the two are layered rather than interchangeable. `urlutil` is syntactic and offline; `SSRFGuard` resolves hosts and blocks private ranges. Its only consumer is azd-app's service config validation, where the URLs are local dev endpoints that `SSRFGuard` would reject by design, and where DNS lookups during config parsing would be wrong. `ValidateDomain` was also checked against `azdext.ValidateHostname`: the label rules match, but `ValidateDomain` additionally rejects a port and requires at least one dot, and reports which character failed. Delegating would loosen validation and degrade the message. SSRF adoption belongs in task 2.10 (`httpclient`), where outbound requests are actually made. | 2.0 |
| [x] 2.7 | C | **Trimmed `fileutil` internals, kept the exported API.** `atomicWrite` and `renameWithRetry` deleted; `AtomicWriteJSON`, `AtomicWriteFile`, and `EnsureDir` now delegate to `azdext.WriteFileAtomic` / `azdext.EnsureDir`. The plan also said to delete `AtomicWriteFile` and `EnsureDir`, which verification rejected: `fileutil.DirPermission` is `0750` and `azdext.EnsureDir` falls back to `0755`, so deleting the wrapper would have loosened directory permissions at 24 call sites across three repos, each of which would have had to remember the policy. Keeping one-line wrappers that carry a security-relevant default is cheaper and safer than spreading it. Net win is real: the SDK's `osutil.Rename` retries the actual Windows lock errors (`ERROR_SHARING_VIOLATION`, `ERROR_ACCESS_DENIED`) for up to 10s, where `renameWithRetry` retried *any* error for 200ms, and `WriteFileAtomic` handles `perm == 0` instead of chmod'ing the file to `0000`. Whole existing suite passes unchanged. fileutil 89.0% -> 95.5%. | 2.0 |
| [x] 2.8 | C | Trim `security`. **Plan claim refuted.** The SDK has no standalone `ValidatePath` (~70 call sites), `ValidatePathWithinBases` (returns a resolved path `MCPSecurityPolicy.CheckPath` does not), `ValidatePackageManager`, or `ValidateFilePermissions`. Only 3 of 7 functions could delegate: `ValidateServiceName` (format rule only, keeps `allowEmpty`, the length message, the sentinel, and the `..` check the SDK regex accepts), `SanitizeScriptName` renamed to `ValidateScriptName`, and `IsContainerEnvironment` | 2.6 |
| [x] 2.9 | C | Rebase `keyvault` internals on `azdext.KeyVaultResolver`. Kept the warning-collecting façade. **One gap the plan missed:** `azdext.ParseSecretReference` accepts only the three segment `akvs://` form, but azd-core documents a four segment form carrying a version, so `normalizeReference` rewrites it into the equivalent `@Microsoft.KeyVault(VaultName=...;SecretName=...;SecretVersion=...)` reference | 2.0 |
| [x] 2.10 | C | **Keep `httpclient.Client` and `handlePagination`; harvest the SDK behavior they were missing.** Verified: neither `azdext.ResilientClient` nor `azdext.Pager` can express what azd-rest needs. `ResilientClient.Do` has no per-request headers, no TLS opt out, no redirect policy, no response size limit and no verbose logging, it takes an `azcore.TokenCredential` rather than azd-core's scope-string `TokenProvider`, and it derives the scope from the URL instead of honoring `--scope`. `azdext.Pager[T]` only reads `nextLink`, so adopting it would delete `@odata.nextLink` (Microsoft Graph), `@odata.next`, and RFC 5988 `Link` header pagination, and its `HTTPDoer` cannot carry the auth or custom headers a page request needs. What was adopted: `azdext.SSRFSafeRedirect` on the redirect path (the redirect target is chosen by the server, so it belongs under an SSRF policy), plus retry on 429 and 408, `Retry-After` support, jittered backoff, and a bounded body drain before each retry | 2.0 |
| [x] 2.11 | C | `auth`. Plan was wrong twice. `azdext.ScopeDetector` is not a superset of `DetectScope`: it lacks 10 of azd-core mappings and disagrees on `.azurecr.io`, so deleting `DetectScope` would have broken Log Analytics, Batch, SQL, Synapse, Data Lake, Media Services, MariaDB, table and file storage, and pointed ACR at the wrong audience. It also cannot express the Kusto per-cluster scope or the Service Bus versus Event Hubs path split. Layered instead: dynamic rules stay local, everything else delegates via `CustomRules`. Gained Azure OpenAI, Cognitive Services, and AI Services, which returned no scope before. `azdext.TokenProvider` is likewise not a drop-in: no cache, no error classification, azd CLI credential only. Kept `AzureTokenProvider` as the front end and added `NewAzureTokenProviderWithCredential` plus `NewAzureTokenProviderForHost` so a host supplies the tenant while caching and classification are preserved. `IsAzureHost` kept: azd-rest uses it as the authenticate-or-not gate and there is no SDK equivalent | 2.10 |
| [x] 2.12 | C | `logutil`. Not deprecated, layered. `azdext.LoggerOptions` carries a single `Debug` boolean, so it cannot express warn or error, and `SetLevel(LevelError)` would have kept emitting info lines. `azdext.NewLogger` also does not inherit `slog.Default` despite its doc saying so, so a naive delegation would have made `SetOutput` a no-op and leaked every captured log line to stderr. `ComponentLogger` now wraps `*azdext.Logger`, built with the package writer and format, and filters by the package level. Gained `WithComponent`, `AzdextLogger`, `Slogger`. Setup functions delegate to `azdext.SetupLogging` for the levels it can express. Fixed a real bug: `AZD_DEBUG` was compared to the literal `"true"`, so `AZD_DEBUG=1` did nothing even though the framework honors it | 2.0 |
| [x] 2.13 | C | `cliout`. `PrintJSON` and `Table` now delegate to `azdext.Output`; `Table` flattens its `map[string]string` rows into the SDK's positional shape and gains JSON mode, which it silently lacked before (a text table was printed into the JSON stream). Color and prompt gating now come from `azdext.DetectInteractive()`. Fixed two real bugs found while doing it: `noColor` was hardcoded `false` and `getNoColor` was never called, so `NoColor()` was inert and ANSI escapes leaked into redirected output; and `Confirm` consulted only `IsJSON()`, so it blocked on `fmt.Scanln` under CI, `AZD_NO_PROMPT`, and AI agent hosts. All 43 exported functions kept | 2.0 |
| [x] 2.14 | C | `env`. Delegation rejected, and the rejection is now pinned by tests. `azdext.LoadAzdEnvironment` runs `azd env get-values` with no `-e` flag, so it always reads whichever environment azd considers current. Swapping it in would have made `-e staging` silently load the default environment, which is the exact bug `env.LoadAzdEnvironment` exists to prevent. The SDK version also drops the JSON path, the environment name allowlist, and the injectable `CommandRunner`. A probe further showed `azdext.ParseEnvironmentVariables` trims values and keeps single quotes, so it changes two legitimate value shapes. No `LoadAzdEnvironmentFromSubprocess` was added: the detached-child skip depends on azd-app's own spawn marker and belongs to azd-app | 2.0 |
| [~] 2.15 | C | Release `azd-core v0.6.0`. **Plan step corrected:** `CHANGELOG.md` is generated by `.github/workflows/release.yml` from commit subjects and prepended on every release, so a hand-written section would be clobbered on the next run. The accumulated table is instead published as the user-facing guide `docs/migration-v0.6.0.md` and linked from the README; `docs/specs/azdext-alignment/migration.md` is now a pointer stub. Code, docs, and guide are ready. **Blocked on owner approval to tag and publish** | 2.1-2.14 |
| 2.16 | A P R | Bump `azd-core` to `v0.6.0` and fix all call sites | 2.15 |

## Phase 3: Capability uplift

### azd-app

| # | Task | Depends on |
|---|---|---|
| [x] 3.1 | **Removal rejected, test delivered.** The guard is load-bearing and azdext does not obviate it: a detached child outlives the azd host that served its parent, so `azd env get-values` blocks and leaves an empty `run.log`. `azdext.LoadAzdEnvironment` runs the same subprocess and does not even pass `-e`. Extracted `commands.IsDetachedChild` into a package-level variable and pinned both sides of the condition in `detached_env_test.go`. `src/cmd/app` coverage 24.0% -> 60.0% | 2.16 |
| [x] 3.2 | **Replacement rejected, bug fixed instead.** `azdext.ConfigHelper` cannot stand in: it wraps no `GetSection` RPC (which `GetAllServicePorts` needs), its setters `json.Marshal` the value so every stored key would change representation, and it is a concrete struct with no interface, so it cannot replace the `ConfigClient` seam that `dashboard.Server` and `portmanager.PortManager` hold as a field, nor `InMemoryClient`, which is both the test fake and the production fallback when azd's gRPC server is absent. `ProjectHash` also drives on-disk paths under `~/.azd/azd-app/<hash>/`. Investigating it surfaced a real defect: a service name containing `.` split the config path into a nested object that `GetAllServicePorts` silently dropped, and ConfigHelper's path validator permits dots so it would not have caught it either. Added reversible percent-encoding of the service-name segment; safe names encode to themselves so existing keys still resolve. `azdconfig` coverage 47.1% -> 54.9% | 2.16 |
| [x] 3.3 | **Embedding rejected, contrast pinned.** None of the methods are no-ops. Each returns a non-nil success sentinel so the azd host records the local service as handled and continues deploying the rest of the project; `GetTargetResource` returns a synthetic `local` resource. The SDK base answers every method with nil, so deleting them turns "skipped cleanly" into "produced no result". Embedding for forward compatibility is also wrong here: it would silently give any future interface method the nil default, whereas an explicit implementation makes a new method break the build and force a decision. `base_provider_divergence_test.go` asserts the base returns nil where this provider returns non-nil | 2.16 |
| [x] 3.4 | **Adopted as written.** Extracted the inline configure closure in `listen.go` into `ConfigureExtensionHost` so it is addressable from a test. `providers_manifest_test.go` verifies `extension.yaml` against the runtime registrations, and proves the check can fail in both directions (declared-but-unregistered, registered-but-undeclared) so it cannot silently rot into a no-op. A fourth test pins that registration stays lazy, which the verifier depends on | 2.16 |
| [x] 3.5 | **Policy rejected, audit delivered.** `WithSecurityPolicy` is a carrier only: the builder stores it and enforces nothing, so a policy is decorative unless handlers call it. Its URL half is entirely inert here because no MCP tool takes a URL and none makes an outbound request. Its `CheckPath` is strictly weaker than the existing `validateProjectDir`: it is a complete no-op when no base paths are configured and `DefaultMCPSecurityPolicy()` configures none, it does not require the path to exist or be a directory, it has no leading-dash guard (CWE-88, critical here because the value reaches a subprocess as `--cwd <path>`), no system-directory denylist, and it fixes its base list at construction instead of recomputing cwd per call. Delivered the audit half as `mcp_security_audit_test.go`: 22 subtests prove all 11 path-taking tools reject hostile input, a classification table fails the build when a tool declares an unclassified parameter, and a test fails the day a URL-shaped parameter appears. Adopted the one idea the policy had that was missing, an audit trail, as a `slog.Warn` on rejection with the path sanitized for CWE-117. Writing it exposed a pre-existing defect: four sanitization scan loops asserted `c.(interface{ GetText() string })`, but `mcp.TextContent` has no such method, so every ANSI check was vacuous. All four now route through a helper that fails on empty input | 2.16 |
| [x] 3.6 | **Adopted, with a rewrite.** `azdext.NewMetadataCommand` marshals `GenerateExtensionMetadata` immediately and exposes no hook for `Configuration`, so the command was rebuilt around the same generator. Project and service schemas are reflected from `service.AzureYaml` and `service.Service` with `FieldNameTag: "yaml"`, because those structs mostly carry no json tag and the reflector default would publish Go field names (`Command`) instead of real config keys (`command`); generating from the structs means a new azure.yaml field reaches the schema with no hand edit. The four azd-owned top-level keys are stripped after reflection so the extension does not claim fields azd validates. The global schema is hand-modeled because no Go type describes the user config tree `azdconfig` writes, with ports typed string-or-integer to match `GetAllServicePorts`. `TestEveryAzdAppEnvVarIsDocumented` scans non-test source for `AZD_APP_*` names and fails when one is undocumented; it found `AZD_APP_INSTANCE` and `AZD_APP_DETACHED` missing on its first run | 2.16 |
| [x] 3.7 | **Both rejected, decision pinned, real gap closed.** `WithProvisioningProvider` is the extension point for alternative IaC engines: eight methods covering the whole infrastructure lifecycle, routed by the infra provider name in azure.yaml. azd-app has no infrastructure, so seven would be stubs and a user selecting it would get local processes instead of Azure resources; the `local` service target already registered is the correct integration for a local runtime. `WithValidationCheck` runs inside every `azd provision` for every user with the extension installed, including users who never run `azd app`, and everything azd-app can check is local (tool installation, port availability, command syntax), so it would gate a cloud operation on a local reason; the existing `lifecycle-events` capability already offers a `preprovision` handler if a real need appears. Both decisions are pinned by tests that fail on adoption so the rationale must be read. The evaluation also surfaced a real gap: azd gates on the manifest `capabilities` list before it looks at `providers`, but `azdext.VerifyProvidersMatchManifest` compares only providers, so a provider can be declared and registered correctly and still be unreachable with no error anywhere. `TestCapabilitiesMatchRegisteredProviders` now checks both directions | 3.4 |

### azd-copilot

| # | Task | Depends on |
|---|---|---|
| [x] 3.8 | **Rejected on capability, not latency.** Latency was never the deciding factor, because there are not two comparable paths to time. `SendCopilotMessageRequest` carries prompt, session_id, model, reasoning_effort, system_message, mode, debug and headless, and the whole copilot proto adds only working_directory on session listing. That leaves four blockers. No agent selection, though every launch passes `--agent` and the repo ships 16 of them. No custom instructions directory, though the repo ships over 150 skill directories that the CLI loads on demand by name through `COPILOT_CUSTOM_INSTRUCTIONS_DIRS` and `--add-dir`; folding that catalog into `system_message` would send everything on every turn and still lose the skill invocation tool. No environment injection, which is how the agents receive project, service, subscription, tenant and `AZURE_*` context. And no assistant text on the response, which returns only session_id, usage and file changes, so reading the reply needs a second `GetMessages` call and the unary call produces no incremental output during a long run. `grpc_evaluation_test.go` pins each fact and fails the day the SDK gains an agent, directories, environment or response-text field | 2.16 |
| [-] 3.9 | **Does not apply.** Conditional on 3.8 being favorable. It was not | 3.8 |
| [x] 3.10 | **Adopted as new capability.** The task read as a migration but there was nothing to migrate: azd-copilot had no model or quota lookup of any kind, and its `--model` flag names a GitHub Copilot model, not an Azure OpenAI deployment. The useful version is grounding. An agent scaffolding an Azure OpenAI app otherwise guesses the model name, the region it is offered in and whether the subscription has capacity, and a wrong guess surfaces as an opaque ARM quota failure it then has to diagnose. Added three read-only MCP tools over `client.Ai()`: `list_ai_models` (versions, SKUs and the full capacity range, since an agent picking a deployment size needs all four bounds), `find_ai_model_locations_with_quota` (regions with headroom for a named model, so the region is chosen before provisioning rather than after a failure) and `list_ai_quota_usage` (current, limit and precomputed remaining). Two findings came out of it. `azdext.ToolArgs` has accessors for string, int, bool and float but none for arrays, so array arguments must be read off `Raw()`; a local helper does that and a test fails the day the SDK adds its own. And `ToolArgs` holds an unexported map, so outside the SDK it can only be built through `ParseToolArgs` on an MCP request, which every extension test needs to know. Registration is guarded in both directions: a tool that is written but never wired produces no compile or runtime error, only silent absence from the toolset | 2.16 |
| [x] 3.11 | **SDK policy rejected again, a real gap found and closed instead.** The task 3.5 refutation holds here and is stronger: `WithSecurityPolicy` is a carrier the builder stores and never enforces, and azd-copilot exposes no tool parameter that is a path or a URL, so both of its checks would act on nothing. The audit that produced that answer found something the policy would never have caught. `run_workflow` accepts a `steps` array and forwards each step's args straight to azd's root cobra command, and neither side applies an allowlist: the gRPC workflow service converts the steps and runs them, so any azd command is reachable, including hidden ones such as `auth token`. Nothing shells out and the response is empty so no output returns to the model, which makes this a consent problem rather than an execution one: whoever approves a call named `run_workflow` is agreeing to a project lifecycle sequence, not to arbitrary azd invocation. Added a narrow guard rejecting the command groups that mutate credentials or machine-global state (`auth`, `config`), plus a leading-dash check so an injected flag cannot stand in for a command name (CWE-88), and corrected the tool description, which previously said only "Execute an azd workflow". Destructive lifecycle commands like `down` stay permitted because they are legitimate steps and the tool is already marked destructive. Guards fail if the check is unwired, if the denylist is emptied, if a description drifts from behavior, or if any tool ever declares a path or URL parameter, which is the condition that would make the SDK policy worth re-evaluating | 2.16 |
| [x] 3.12 | **Half the task applied, and the half that did was the valuable one.** The command delegated to `azdext.NewMetadataCommand`, which marshals `GenerateExtensionMetadata`'s result immediately and offers no hook for `Configuration`, so it published commands and nothing else. Rebuilt around the same generator, matching task 3.6. The JSON Schema half does not apply: azd-copilot persists no user configuration (the context command reads azd's whole user config for display but writes none of it) and defines no `azure.yaml` keys at either scope, so all three schema fields stay nil rather than shipping empty objects that would claim a surface that does not exist. The `EnvironmentVariables` half turned out to matter more than the task implied. azd-copilot sets fourteen variables on the GitHub Copilot CLI process, and the agents and skills it ships read them to know which project, subscription and infrastructure they target, which made those names a contract between the extension and its own prompt content with nothing stating it anywhere: a rename in `launcher.go` could strand every skill on the old name and no build would fail. Documented all fourteen plus the one input variable, excluding `AZD_SERVER` and `AZD_DEBUG` (azd's) and the forwarded `AZURE_*` values (the project's). Tests walk the source in both directions so an undocumented variable and a stale entry both break the build, and separate input from output variables since a reader needs to know which names change behavior. Guards fail if a schema appears without the extension actually writing configuration, if the command regresses to dropping `Configuration`, or if the extension id drifts from `extension.yaml` | 2.16 |

### azd-rest

| # | Task | Depends on |
|---|---|---|
| [-] 3.13 | ~~Delete custom pagination in `src/internal/client`, use `azdext.Pager`~~ **Rejected: the task names code that does not exist here.** The code named here does not exist in azd-rest. `src/internal/client` is a 38-line re-export shim over `azd-core/httpclient` created by Phase 1; the real `handlePagination` lives in azd-core and was already evaluated and kept in task 2.10, where `azdext.Pager[T]` was refuted over five gaps. Confirmed no nextLink handling remains anywhere in azd-rest. The plan was written against the pre-Phase-1 layout and never rebased. | 2.16 |
| [-] 3.14 | ~~Delete custom retry and token caching in `src/internal/cmd/mcp.go`, use `azdext.ResilientClient` and `azdext.TokenProvider`~~ **Rejected: nothing to delete.** azd-rest has no custom retry at all. It passes a `Retry` count to azd-core's client, whose `ResilientClient` swap was refuted in 2.10 over eight gaps; `TokenProvider` was refuted in 2.11. The token and HTTP client caching is correct, including the non-obvious part: a non-default `timeoutSeconds` bypasses the cached client in `getOrCreateHTTPClient`, so a per-request timeout is never silently ignored. Checked specifically because that is the bug this shape usually has. | 2.16 |
| [-] 3.15 | ~~Use `azdext.ScopeDetector` for scope detection~~ **Rejected: already done.** Already uses `auth.DetectScope` from azd-core, and `ScopeDetector` was refuted in 2.11. No work to do. | 2.16 |
| [x] 3.16 | Reframed: prove the MCP security policy is enforced on every tool. **Path validation rejected a third time; the enforcement itself is now pinned.** `ValidatePathsWithinBase` rejected a third time: enumerated all six tools and every parameter (`url`, `body`, `scope`, `headers`, `timeoutSeconds`, `retry`, `maxResponseSizeBytes`, `noAuth`) and found zero filesystem paths, so it would guard nothing. The URL half was already adopted. The real gap was that enforcement is a convention no test protects: `WithSecurityPolicy` is a carrier only, the builder never applies `CheckURL` to arguments and cannot, since it has no idea which argument is a URL. Added `mcp_security_surface_test.go`, six tests driving every tool the server actually registers, so a future tool that skips `policy.CheckURL` fails the build. Caught a false pass in my own test first: a connection refusal looks identical to a policy refusal, so it now asserts the refusal cause. | 2.16 |
| [x] 3.17 | Add configuration JSON Schema and `EnvironmentVariables` to the metadata command. **Configuration published, and the variable list generates itself.** Rebuilt `metadata.go` around `GenerateExtensionMetadata` because `azdext.NewMetadataCommand` marshals immediately and drops `Configuration`. The env var half mattered more than the task implied: azd-rest names its variables nowhere, `applyEnvDefaults` derives `AZD_REST_<FLAG>` from every extension flag, so there was no list to publish because there was never a list. Generated it from the same flag names and `configEnvVarName` the config command uses, so a new flag publishes itself. Schemas stay nil: nothing is persisted, no `UserConfig().Set`, no `azure.yaml` keys, verified by a source scan rather than asserted. 12 tests, all guards negative-tested. | 2.16 |

## Phase 4: Distribution

| # | Repo | Task | Depends on |
|---|---|---|---|
| [x] 4.1 | A P R | Replace `minAzdVersion` (azd-rest, not a schema field) and add `requiredAzdVersion: ">= 1.29.0"` to all three manifests. Validate against `extension.schema.json` **Confirmed and generalized.** `minAzdVersion` is read by nothing: absent from `extension.schema.json`, absent from azd's `ExtensionSchema`, and since the schema does not set `additionalProperties: false` and the loader discards unrecognized keys, it validated, loaded, and did nothing. azd-rest was advertising no constraint while appearing to declare one 19 minor versions stale. `requiredAzdVersion` is real and load-bearing: `azd x publish` copies it into `registry.json`, where `IsCompatibleWith` filters versions at install and upgrade time. Rather than hand-checking three manifests once, added `azd-core/manifest`, which parses into a node tree so keys a typed decode would discard survive to be reported, and holds the declared floor equal to the azure-dev module in `go.mod`. Equality rather than satisfiability, because an extension compiled against a given azdext calls gRPC services only that release serves, so the module version is the floor; a floor too high inconveniences someone, a floor too low breaks them. Each extension now has two guards, both negative-tested against its real manifest. Also confirmed `azd x` never writes `extension.yaml` back (`pack.go` copies it verbatim), so the `mcp:` block, which `ExtensionSchema` does not model, survives packaging. Left azd-rest's inert `author`, `homepage`, `license` and `commands` keys in place but named them in the exception list so they are a decision rather than an accident. | 3.x |
| [x] 4.2 | A P | Standardize magefiles on `azd x build` / `pack` / `publish` / `release`, matching azd-rest. **Premise was wrong, gap was real.** Nothing was unstandardized where it ships: all three repos already call `azd x build --all`, `azd x pack`, `azd x release` and `azd x publish` from their release and pr-build workflows, identically. And azd-rest has no `Release` target, so there was no such standard to match. The real gap was local: azd-app and azd-copilot could only `mage build`, which installs a bare binary and never exercises packaging, so the thing a developer tested was never the thing a user installs. Added `Pack`, `Publish` and `Setup` to both, pointed at the repo`s own `registry.json` so this path cannot reach a real registry. azd-app also patches the dev version into `extension.yaml` first, because `azd x pack` reads the version from there and would otherwise name the archive after a version the binary does not report. Left CI as the canonical publish path rather than routing workflows through mage: one path that ships, one that tests installs. Fixed a latent race in azd-rest while matching it, `mg.Deps` runs in parallel, so `Setup` could pack before the build finished. | 4.1 |
| 4.3 | new | Create a `jongio.azd` extension pack manifest with dependencies on the three extensions | 4.1 |
| 4.4 | A P R | Submit entries to `registry.dev.json` in Azure/azure-dev with SHA256/SHA512 checksums | 4.3 |

## Verification

Run after each phase, not just at the end.

- `mage test` green in all four repos, zero warnings.
- `mage lint` / `golangci-lint` clean.
- `azdext.ValidateNoReservedFlagConflicts` and `VerifyProvidersMatchManifest` tests pass.
- Manual smoke on Windows, Linux, macOS: `azd app run`, `azd rest get <url>`, `azd copilot -p "hello"`.
- `go build` binary size delta under 10 percent per extension.
